# PostgreSQL FTS

## Generated Vector

The ginanchor fixture verifies that search_vector is generated by the database layer.
The migration creates a GIN index and uses the simple text search dictionary.

## Reindex Scope

MVP has no dedicated reindex or rebuild command.
