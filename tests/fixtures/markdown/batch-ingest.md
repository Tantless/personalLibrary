# Batch Ingest

## Directory Report

The batchanchor case confirms non-recursive directory ingest continues after file failure.
The report must include succeeded, skipped, and failed item lists.

## Scope

Directory ingest accepts local file paths only and ignores nested directories.
