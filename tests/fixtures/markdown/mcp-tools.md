# MCP Tools

## FastMCP Surface

The toolanchor document lists health_check, ingest_source, search_knowledge, read_source, and get_context_pack.
Each tool delegates to the same application services used by CLI commands.

## Local Default

MVP tools are intended for local-only usage by default.
