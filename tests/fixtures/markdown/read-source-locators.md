# Read Source Locators

## Line Ranges

The lineanchor scenario uses unified line ranges as the primary locator.
read_source can return the exact chunk plus optional surrounding context lines.

## Citation Shape

The caller can address evidence by chunk id or by source id, version id, and locator.
